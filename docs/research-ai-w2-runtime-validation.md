# RA-05/W2 — fake implementation and runtime validation

**LOCAL W2 FIX CANDIDATE / INDEPENDENT REVIEW PENDING.** The fake-only implementation starts at `27ee5948af8ea3569dd5f934610356a6a32afc26` on `codex/ra-05-w2-implementation`. The current narrow fix continues reviewed implementation `eefab27233a70b92ab3466673e165c3a4a2d5e5e`, separating the SQL crash worker's client connection target from its parent-pinned server identity. The accounting uncertainty, immutable observer identity and SQL-owner crash fixes remain intact. No push, PR, merge, deployment, provider/Target call or paid usage is part of this work.

[PR #153](https://github.com/runyiy/ai-api-security-platform/pull/153) previously integrated the N1 prerequisite at `eb69379910b99cf312cbc9b08f9a3c4b72088c9d`. The hosted results retained here establish that prerequisite, not CI acceptance of this new local implementation.

## Adoption and scope

[B1–B8](research-ai-budget-contract.md#w2-design-adoption-record) and [concrete implementation contract v0.1.1](research-ai-w2-implementation-contract.md) retain their design and acceptance authority. The latter was reviewed at `302b1e581f4e0e559a0f16f924d1ca64ea985adb` and integrated through [PR #152](https://github.com/runyiy/ai-api-security-platform/pull/152).

The User subsequently replied **“采纳”** to N1 v0.1.1, including deployment-wide acceptance closure and explicit recovery before reopening. The later continuation handoff records **“采纳”** for mandatory Linux/bubblewrap isolation, subject to all four conditions:

1. Required isolation must fail when unavailable, with no skip, ordinary-subprocess fallback or in-process substitute.
2. Validate the complete sandbox configuration with adversarial ownership/control-boundary tests.
3. Database authentication/privileges must prevent administrative identity switching and server-side filesystem/program bypasses.
4. Demonstrate compatible database access in hosted CI without sharing the host network or weakening its gates.

No observation/message timestamp, signature, identity or approval ID is supplied for these subsequent decisions, and none is inferred. [B1–B8 adoption](research-ai-budget-contract.md#w2-design-adoption-record) remains effective. Design adoption supplies no operational account/key, private-data/retention/egress, spending, deployment, real-provider or Target approval.

The subsequent **“Adopted CI-only bwrap compatibility policy”** User handoff explicitly adopts the exact Ubuntu `bwrap-userns-restrict` profile and authorizes implementation without repeat approval. [The bounded adoption and implementation record below](#adopted-ci-only-apparmor-compatibility-policy) records its package/hash, job-wide executable scope and verified hosted integration. This is an adopted CI prerequisite, not W2 completion or operational authority.

The earlier same-UID probe showed a child could chmod/replace parent-created 0400 synthetic content. It motivated the adopted OS boundary; same-UID subprocess/file modes are not an acceptable isolation fallback. Historical probe details remain in the [integrated development record](https://github.com/runyiy/ai-api-security-platform/blob/f5ac48e2a9b4f6ca3c85cd7923f930e1ee5b04b3/docs/research-ai-w2-runtime-validation.md); temporary directories are not prerequisites for understanding or rerunning current validation.

## W2 implementation and acceptance

The [internal package](../backend/app/ai/w2/) implements the adopted concrete v0.1.1 protocol. A trusted synthetic harness explicitly constructs it; there is no environment enablement switch, public route, cache, worker or task loop. Existing W1 schemas, digests, prompt/profile, default-disabled configuration and standalone fake qualification remain in force.

[Preparation](../backend/app/ai/w2/preparation.py) implements Q1–Q4 and the complete literal P. Q1–Q3 use fixed templates/references with zero model calls. Q4 performs one bounded retrieval and can prepare only the declared unresolved pair. Missing inputs remain local codes. [Retrieval](../backend/app/ai/w2/retrieval.py) filters shared synthetic metadata before loading/ranking, requires genuine current W3 proof and separate operator publication, and verifies independent projection decisions against the complete content/example/counterexample binding. Consumer ownership/DATA/permission, lineage and withdrawal stay current. Shared synthetic consumption does not borrow the author's old project/Target permission.

[Migration `7ba5dce4a936`](../backend/alembic/versions/7ba5dce4a936_add_research_ai_budget.py) adds 17 unseeded tables, immutable history, exact keys, active-slot indexes and the lifecycle acknowledgement backstop. Account→task→call locking conserves settled and held amounts atomically. Known over-cap usage is charged in full; unknown or conflicting evidence pauses admission. Corrections require explicit recovery and post only deltas. Metadata contains no durable prompt, response or credential, and no source FK obstructs DATA deletion. Populated downgrade refuses to erase evidence.

[The parent authority](../backend/app/ai/w2/fake_authority.py) serializes tickets, permits, endpoint acceptance and deployment closure under A. Journal fsync precedes acknowledgement; a separately retained endpoint witness records acceptance IDs and journal high-water evidence. [Bounded IPC](../backend/app/ai/w2/ipc.py) exposes one call and one connection. [Execution](../backend/app/ai/w2/execution.py) starts a fresh interpreter in the enforced sandbox. The child has no authority control methods, administrative credentials, journal/witness paths or host networking. Actual W2 execution needs **zero child database grants**; a separate restricted-role test checks all actual W2 tables/functions, beyond the old counter probe.

Accounting updates the locked in-memory and SQL projections together. Overflow, conflicts, coverage loss and cleanup preserve prior settled usage and conservative liability. Reconciliation checks the bounded independent inventory, including contradictions outside the selected copy batch, and rechecks it after acknowledgements and settlement. An explicit correction atomically records its settlement and a `ra-w2-recovery-resolution/1` event in the existing immutable event table. That event binds one decision, chosen final, settlement and exact observer-prefix count/digest. Ordinary replay cannot resolve a dispute; an already used decision cannot authorize later contradictory evidence. Recovery inventory and budget resumption verify this provenance against the surviving independent history. This fix requires no additional migration.

The observer preserves the first event's identity and complete envelope. Identical redelivery returns it without growing either journal. Conflicting reuse appends a separate `CONFLICT` observation containing the incoming usage/terminal/binding and the original observation's digest, suspends admission, and returns that explicit conflict from the parent-only `late_usage` fixture method so the caller can reconcile it. The changed contents never become a `FINAL_USAGE` replacement; repeated conflicting delivery returns the same conflict. Contradictory late finals with a fresh ID and exhausted observation capacity also suspend acceptance before another admission can consume stale SQL balances. SQL copy timing does not affect this behavior. Complete coverage means all durable evidence is present; it does not make contradictory totals trustworthy. Reconstruction rejects historical duplicate-ID journal frames without rewriting either original frame.

| Requirement | Implementation and observable validation |
| --- | --- |
| T1 | Fixed policy, reviewed question/task manifests and P; [preparation tests](../backend/tests/ai/test_w2_preparation.py) count retrieval invocations and parent endpoint acceptances. Only Q4 has a prepared call; every local missing code has zero provider preparation. |
| T2 | [Strict records](../backend/app/ai/w2/records.py), preallocation decoder and IPC; [boundary tests](../backend/tests/ai/test_w2_boundaries.py) exercise exact/+1 bytes, nodes, depth, identifiers, versions, generations, selections, metadata scans, events and pending operations. [Security tests](../backend/tests/ai/test_w2_security.py) reject oversized headers before body loading and exclude unselected canaries. Individual schemas retain their tighter cardinalities. |
| T3 | Exact source/scope/decision bindings and genuine W3/publication; preparation, boundary and [writer tests](../backend/tests/ai/test_w2_writers.py) cover withdrawal, closed context, revoked projection/candidate/manifest, test-only publication and nested reads. Public retrieval keeps its existing fact/subject checks. |
| T4 | [Budget store](../backend/app/ai/w2/accounting.py) and [accounting tests](../backend/tests/ai/test_w2_accounting.py): independent Decimal calculations, both PostgreSQL balances and all four postings; exact/null/zero caps, full keys, cache rates, over-cap actuals, disputes and integer overflow. Unrepresentable final cost retains exact observed counts and freezes admission. |
| T5 | Competing real PostgreSQL transactions with independently observed lock waits, one winner, rollback and lost commit acknowledgement, exact lookup, unique question/call keys and active slots. |
| T6 | [Absolute deadlines](../backend/app/ai/w2/time.py) retain every qualified dependency window. Final-return/complete tests cover a 0.5s lookup within a one-second source window, equality, cancellation and wall/monotonic rollback, preserving known usage. |
| T7 | [Process tests](../backend/tests/ai/test_w2_processes.py) include nine SQL-owner SIGKILL cases: reserve, marker and settlement, each before commit, after database commit before BudgetStore API acknowledgement, and after acknowledgement. The [trusted coordinator](../backend/tests/ai/w2_sql_worker.py) owns those transactions and the marker G session. Fresh physical connections inspect the pinned mutation transaction's `txid_status`, session disappearance, balances and postings. Separate journals/witnesses survive unchanged. Nine earlier sandbox-adapter deaths remain adapter/transport coverage; the parent owns their SQL. |
| T8 | [G/controller](../backend/app/ai/w2/guard.py), independent A generations and call ownership; [lifecycle tests](../backend/tests/ai/test_w2_lifecycle.py) terminate G, commit context/publication invalidation and resume the same unexpired child permit with unchanged owner and zero new admissions. Recovery/finish tests preserve the successor slot. |
| T9 | [Uncertainty regressions](../backend/tests/ai/test_w2_uncertainty.py) cover overflow/ordinary/unknown event orders, batch boundaries, cleanup, greater known lower bounds, acknowledgement/settlement races and stale resolution restoration. [Observer identity tests](../backend/tests/ai/test_w2_observer_identity.py) cover identical/conflicting reuse before/after SQL copy and journal restart, acknowledgement loss, terminal conflicts and legacy duplicate frames. Explicit correction adjusts only the delta and retires one exact evidence prefix; new conflicts require a new decision. Trusted late usage never revives cancellation or display. |
| T10 | [Recovery tests](../backend/tests/ai/test_w2_recovery.py) omit result/accounting/both, core/balance snapshots, marker/postings and observer state. Independent journal/witness comparisons expose omissions; missing independent evidence cannot establish completeness. |
| T11 | [Recovery](../backend/app/ai/w2/recovery.py) checks pending transactions, exact call evidence, conservation, postings and stream closure. Tests cover fsync/ack loss, older restoration, a lost-G writer still able to commit, a writer paused after its successful G check, changed recovery generations, final recovery expiry, cancellation, bounded capacity and explicit budget resumption. Unattributed settled balances are refused. |
| T12 | Exact MemoryWire permit port and narrow terminal hook in [W1 wrappers](../backend/app/ai/w2/w1_bridge.py), actual sandbox/IPC forgery and [W2 privilege negatives](../backend/tests/ai/test_w2_privileges.py), and existing W1 regressions. The complete-rendering fake counter certificate is not a provider tokenizer or billing/overhead proof. |

| Trace | Implementation and independently observable evidence |
| --- | --- |
| X1 | Execution/settlement: `test_x1_y5_real_sandbox_success_and_atomic_postings` records one parent endpoint acceptance and checks both SQL balances plus all four postings. |
| X2 | Authority stream closure/accounting: `test_x2_independent_zero_keeps_call_slot` covers marked and unmarked calls; witness count stays zero, independent closure/zero events permit refund, and the allocated call remains consumed. |
| X3 | Conservative reconciliation: `test_x3_unknown_retains_liability_and_prevents_new_reservation` and process-kill cases retain U and pause admission when the endpoint accepted without complete final usage. |
| X4 | Absolute deadline/W1 completion: `test_x4_completed_final_lookup_preserves_known_usage` in [boundary tests](../backend/tests/ai/test_w2_boundaries.py) checks completed final reads, exact equality/cancellation/rollback, suppressed display and preserved independent usage/postings. |
| X5 | Append-only settlement/correction: [accounting tests](../backend/tests/ai/test_w2_accounting.py), uncertainty and observer-identity regressions compare independent original/conflicting records, both SQL balances and immutable postings. Conflicts remain visible before first SQL copy; explicit correction binds a single decision and evidence prefix. |
| X6 | G, A and owner fencing: [lifecycle tests](../backend/tests/ai/test_w2_lifecycle.py) terminate the actual G backend and resume the same sandbox sender; recovery/finish tests verify the newer owner’s slot survives stale cleanup. The Y schedules below identify endpoint counts and closure outcomes. |
| X7 | Independent recovery: `test_x7_omitted_ledgers_recovered_from_parent_core_and_endpoint` in [recovery tests](../backend/tests/ai/test_w2_recovery.py) removes result, accounting, both, or core/balance state while retained parent evidence exposes the call. Separate omission cases remove observer evidence and deny completeness/refund. |
| X8 | Cancellation/recovery ownership: `test_x8_late_usage_requires_recovery_owner_and_cancellation_persists` accepts independently witnessed late usage only through the explicit recovery owner; cancelled state, stale-display denial and no replay persist. |

| History | Actual schedule and observation |
| --- | --- |
| Y1 | write_ready → terminate G backend → durable A closure/ack → context-close or publication-withdrawal commit → stale child resumption. Owner stays 1, permit remains unexpired, admissions stay 1, endpoint count is 0. |
| Y2 | After G loss, the real child sends and acceptance wins A. Its reply is held outside A while invalidation commits. Count is 1; known usage settles, missing usage retains its reservation; display/replay reject. |
| Y3a | Separate control socket drops the reply after durable acknowledgement. No protected database mutation commits; stale acceptance rejects. Independent closed-stream/zero proof is required before refund. |
| Y3b | Control socket drops before delivery. No protected mutation commits; the old permit can accept once. No independent final usage is supplied, so liability remains held and budgets pause. This is not a proven revocation. |
| Y4 | Closing the actual authoritative journal descriptor prevents durable closure acknowledgement and acceptance. No database mutation commits; liability remains held. Restart begins RECOVERY_REQUIRED in a new epoch. |
| Y5 | Unchanged current dependencies and an unexpired permit allow one sandbox request, settlement of `(2432 tokens, 8704 microusd)` and freshly qualified display. |

## Lifecycle integration and limits

[The transaction wrapper](../backend/app/ai/w2/lifecycle.py) obtains G and durable deployment closure before domain locks. Nested services borrow the explicit Session token. Root completion, including internal `commit()`, resolves disposition after SQL ends and releases G once. Late entry on an already used database connection rejects before A and requires caller rollback. Before domain locks, each writer also holds a shared deployment-row lock and checks durable `WriterFence` evidence. Explicit recovery exclusively locks that row, records the closed generation in the existing recovery table, then qualifies current dependencies. This drains already bound transactions and rejects an old invocation paused just after checking G, before its first SQL statement. Recovery pins that original closed epoch/generation and carries the tightest qualified expiry through final A consumption. PostgreSQL statement triggers deny protected writes without the matching transaction/backend acknowledgement copy; that copy cannot issue a permit.

The §5 inventory is integrated at context create/correct/close; observation prepare/accept/read/lifecycle/revoke/maintain; subject record/read; knowledge record/decide/rotate/retrieve; W3 validate/feedback/read/review/invalidate; Target create/network/profile/revision updates; revision create/transition; Scope create; authorization profile mutation/enrollment; and W2 registry/policy/cancellation/recovery. Offline INTENT/verification entrypoints that can indirectly read observations are enclosed too. Already locked indirect callers fail closed at the nested boundary; this adds no Target-network authorization. Pure knowledge query audit and immutable verification history are outside the eligibility mutation set.

Initial synthetic registry installation may batch at most 64 records/65536 bytes under one acknowledged transaction, retaining individual version/current/history checks. Tests also exercise individual replacement/revocation, internal Scope commit, nested W3 disable, outer rollback and lost-G transactions. Uncertain acknowledgement or failed cleanup never automatically reopens the gate.

## Current local validation

Run `python3 -I backend/w2_validate.py complete` from the repository root. This stdlib-only runner copies source without `.env`, `.venv` or operator settings; independently verifies an owned empty PostgreSQL 16 TEST server; then imports/migrates the isolated application. Administrative credentials stay in the parent environment. The printed run directory contains source/identity manifests, migration/test logs, complete/disjoint collection evidence and the owned shutdown result. Test paths select targeted validation through the same checks.

The SQL crash coordinator is a trusted test process outside the adapter sandbox. Before application imports it independently compares the inherited TEST database identity, uses an empty owned working directory and receives only generated disposable credentials. It separately restricts the explicit client URL to `localhost`, `127.0.0.1` or `::1`, with a port, credentials and database, rejecting query/fragment overrides. The complete server identity still matches the parent's database, user, server address/port, data directory and system identifier; the server address/port need not equal the client's published endpoint. It has a bounded call-specific evidence socket to the surviving parent authority. Each `sql-crash-evidence.json` records the killed process, pinned mutation transaction, before/after SQL disposition, independent journal/witness hashes, endpoint count and fresh ledger amounts. The committed-before-ack cases terminate the coordinator after the database driver has returned from commit and before the BudgetStore API returns; they do not claim a lost PostgreSQL wire-level COMMIT reply or a database-server crash. Settlement setup executes the mandatory bubblewrap adapter to obtain actual independent acceptance/final-usage evidence before killing the separate SQL owner.

The subsequent independent review of `eefab27233a70b92ab3466673e165c3a4a2d5e5e` passed **4,015 backend tests in 1,576.44s**, accepted both accounting probes and verified nine SQL-crash evidence sets. It found one test compatibility blocker: an independently matched server reported `127.0.0.2` through a `127.0.0.1` forwarder, and the worker's extra server-address assertion rejected it. The retained probe at `/tmp/rvw2fix-8_9z5q_s/ci-address-probe/` was a local reproduction; hosted CI was not executed. Its unchanged worker bytes match the reviewed commit. That baseline and its artifacts remain untouched.

The [worker identity regressions](../backend/tests/ai/test_w2_sql_worker.py) add **33 cases**: two actual local forwarding positives, all six server-identity mismatches, failed generated-password authentication, 23 malformed/override URL negatives and an operator-file negative. Fresh isolated verifier processes authenticate against an independently verified owned TEST server listening at `127.0.0.2`; both `localhost` and `127.0.0.1` client endpoints use a different forwarded port. Evidence records actual socket endpoints, the pinned and expected identities, zero application imports and relay shutdown. Invalid client targets reject before connection. Raw control characters and noncanonical scheme prefixes reject before URL parsing so parser normalization cannot change the connection target checked by the worker.

Focused run `/tmp/rw2-8j3njpps` passed **102 tests in 68.28s**, with no failures, skips or expected failures: the new 33 cases, 18 existing adapter/SQL crash cases, 10 N1 isolation cases, six W2 privilege/security cases and 35 collection/aggregation helper cases. Empty TEST identity was independently verified before imports/migration: `n1_synthetic`, `127.0.0.1:49409`, system identifier `7684814668398448651`, exact data directory `/tmp/rw2-8j3njpps/pg/data`. Migration to the unchanged head passed. The current complete/disjoint collection contains **4,048 tests = 117 `w2` + 3,931 `remaining`**, intersection 0, SHA256 `a79c895fae6cd1fd2f4151ceb22d8fdfd49c770a345dc7a5ef31aa148cbe53e3`. Runner exit 0 and owned PostgreSQL shutdown were confirmed in 95.254s including setup and collection.

Reproduce the focused tests with `python3 -I backend/w2_validate.py tests/ai/test_w2_sql_worker.py tests/ai/test_w2_processes.py tests/ai/test_n1_isolation.py tests/ai/test_w2_privileges.py tests/ai/test_w2_security.py tests/test_ci_shards.py`. The retained run's `runner.py` is a stdlib-only copy that pins the repository path and inserts `python -m ci_shards check` after migration in the same independently verified environment; test, isolation, statement and process bounds are unchanged. Source manifest SHA256 is `df03c935788fd422156d918ba7399a13d3cfe873ba0bc40cd0c41e2ef4ab09f1`. All 514 backend files match the focused snapshot: 512 are unchanged from the complete-suite baseline, one is the modified test worker and one is its new test file. Application, accounting, migrations, CI workflow and shard definitions are unchanged, so the complete backend suite was not repeated for this test-only fix. These results demonstrate local forwarding compatibility; hosted acceptance remains pending independent review/integration.

The run's `worker-evidence/` archive retains all nine forwarding outcomes and nine SQL-crash evidence sets; all 18 crash journal/witness prefix hashes were independently verified. Archive manifest SHA256 is `07965f45443c6cfd15be744da8f90535b775817e60b99d65b281a283eb7a81a7`. The runner, forwarding, N1 isolation and privileges servers all completed verified shutdown. The three retained clusters were independently rechecked against exact ownership, system identifiers, absent PID files and durable `shut down` state before their data/empty socket directories were removed; `owned-cleanup.json` records them. The privileges fixture removed its own temporary directory after verified shutdown. Source, logs, identities and synthetic evidence remain. No reviewer or operator resources were modified.

The accounting fix run at `/tmp/rw2-2spnwa9e` passed **all 4,015 tests in 1,461.71s (24m 21s)**, with 62 reported collection/deprecation warnings and no skips or expected failures. Migration to `7ba5dce4a936`, migration/metadata checks, W1 compatibility, recovery, lifecycle and mandatory isolation regressions passed. Its collection check established 117 `w2` plus 3,898 `remaining`, intersection 0, including 35 collection/aggregation helper tests. Collection SHA256 is `45f59a4b52e8b065dbb57e23dd0c67926dafcdc50e42ab941bd64fa45aeabcfd`; source manifest SHA256 is `558fa83bdb230c19574c8f8b9ce3605c61446343a7a051b0018bc038e225cea1`. All 513 backend files matched that tested snapshot at commit `eefab27233a70b92ab3466673e165c3a4a2d5e5e`; only documentation evidence changed before that commit. The runner exited 0 in 1,491.798s including setup, collection and confirmed owned PostgreSQL shutdown.

The run verified the empty TEST database `n1_synthetic` at `127.0.0.1:54873`, data directory `/tmp/rw2-2spnwa9e/pg/data`, system identifier `7684795621982515211` before migration or application imports. `identity.json`, `source.json`, `collection.json`, `step-0.log` through `step-2.log`, `result.json` and `source-verified.json` retain the identity, source and validation chain. The nine current-run SQL crash cases are archived under `crash-evidence/`: all 18 journal/witness prefixes match the recorded crash hashes, and the killed process/transaction, fresh SQL disposition, endpoint counts, observation inventory and core bindings were independently checked. Archive manifest SHA256 is `a594b0172d39aa57cf657177eaa4de98a316b1ea265db7cd5f6e2a931e6b331b`.

Focused validation passed **105 cases in 184.58s** at `/tmp/rw2-cdosb5zk`, including accounting, uncertainty, observer identity, recovery, actual SQL-owner crashes and both unchanged reviewer probes. The supplemental source SHA256 was `26ff8e501aed4723fb30bb7faab911f332b04a95a5a4479e402942b7c153f999`; it was copied into the isolated snapshot and removed from the working tree before full collection. Both probes observed reservation `CONFLICT`, task/account `PAUSED_UNKNOWN`, held `(5120,22528)`, settled `(0,0)` and a durable conflict record. The duplicate-ID probe retained complete coverage without treating contradictory evidence as settlement authority. Permanent tests reproduce these failures without the supplemental file.

The broader affected AI/migration run passed **822 tests in 478.47s** at `/tmp/rw2-ozkof5rc`. Later focused snapshots passed 28 uncertainty cases and 77 observer/boundary/accounting cases before the final 105-case run. All these owned servers confirmed shutdown. The accounting fix's complete run superseded those earlier source snapshots. That fix added 52 permanent regression cases, including nine SQL-owner crashes, bringing the package to 198 added cases over the task base before the subsequent 33 worker identity regressions.

Failures and interruption remain recorded. The first SQL-crash run had 17 passes and one recovery-setup failure; it omitted the explicit suspension required before reopening. The first uncertainty run had 68 passes and two fixture failures: scripted unknown usage omits a final observation, so those order tests were corrected to append an actual independent unknown final. The earlier full run `/tmp/rw2-btdabd9h` was deliberately interrupted after 612 passes to fix the reviewer probe's control flow: `late_usage` now returns durable `CONFLICT` evidence for reconciliation instead of throwing before the caller reaches accounting. Its exit 2 and confirmed shutdown are retained. No safety assertion, skip, expected failure, timeout, CI shard or isolation requirement was weakened.

All **11 owned accounting-fix run servers** confirmed shutdown. Cleanup independently rechecked each exact data directory, owner, system identifier, absent PID file and `pg_controldata` state `shut down` before removing only its PostgreSQL data and empty socket directory. `/tmp/w2-fix-owned-cleanup.json` and each run's `fix-cleanup.json` record removal. Source snapshots, logs and synthetic journal/witness evidence remain; the reviewer's `/tmp/rvw2-nt0uxjqo` and operator/shared resources were untouched. That documentation check resolved 84 local references and six explicitly named tests; `git diff --check` passed. These local results await independent review and provide no hosted-CI or live-provider acceptance.

## Validation before the accounting review

Implementation `daa1491643455840c61cae4d4dff17484b9731e7` passed **3,963 tests in 1,901.97s (31m 41s)** before independent review, with 62 reported collection/deprecation warnings and no skips or expected failures. That suite included 146 added acceptance cases but missed the two accounting counterexamples and did not kill the SQL-owning process at commit boundaries. Review subsequently passed 678 targeted tests while both supplemental safety assertions failed: overflow followed by ordinary usage cleared liability, and a reused observer ID replaced `(2432,8704)` with `(1100,3200)` before SQL copy. Those failures motivated the current durable regressions and corrected T7 claims. Migration to `7ba5dce4a936`, metadata checks, privilege negatives and collection passed on that earlier snapshot; current fix results are recorded above.

| Reviewed-parent run evidence | Value |
| --- | --- |
| Reproduction | `python3 -I backend/w2_validate.py complete` |
| Retained artifact directory | `/tmp/rw2-nu9urbpi` (`source.json`, `identity.json`, `collection.json`, `step-0.log`, `step-1.log`, `step-2.log`, `result.json`, `cleanup.json`) |
| Source manifest SHA256 | `2c27f23c7713f0571c7efaf391f9ba417a138612fd6a7cef18c2cd5201e9161d` |
| Full collection SHA256 | `659e0663900178c2b8714031fe8077ae69e57a73de9ca997e5260a07f1336bd9` |
| Disjoint partition | 117 `w2` + 3,846 `remaining` = 3,963; intersection 0; 35 collection/aggregation helper tests included |
| Owned server | `n1_synthetic`, `127.0.0.1:42529`, system identifier `7684768084080218123` |
| Runner result | exit 0, confirmed owned PostgreSQL shutdown, 1,943.492s including setup, collection and shutdown |

All 510 backend files matched that manifest after the run. Only documentation evidence was updated afterward; the source manifest also contains the pre-result documentation snapshot. Temporary artifact paths are local evidence, not prerequisites for reproduction from the review commit.

Focused development runs passed 130 W2 tests, 29 writer/recovery/migration tests after the recovery corrections, and 536 affected legacy regressions after updating their exact Alembic-head expectations. Each confirmed owned shutdown. The final complete run supersedes those earlier source snapshots.

Development failures were corrected: misplaced lifecycle imports, outdated migration snapshots/check names, old Alembic-head assertions outside the migration directory, and an invalid W3 disable-fixture reason. Two complete-suite attempts were deliberately interrupted to correct independently found restoration/recovery races. A later attempt recorded three old-head assertion failures and 1,612 passes before interruption for the fix. No skipped or expected-failure acceptance cases were introduced. Two earlier migration-heavy runs also failed bounded shutdown confirmation; one already had failing migration tests. The runner now performs an owned bounded checkpoint before the unchanged shutdown check. Original logs and unsuccessful shutdown reports are retained.

Owned-resource cleanup removed **22 identified runner databases/source snapshots and 36 additional verified test-cluster directories**. Each cluster identity was checked against `pg_controldata` and its exact pathname socket before removal; all 36 nested clusters had independently confirmed shutdown. The two older bounded-stop failures and the first interrupted full run had inactive endpoints but no confirmed clean shutdown, so their original status remains unsuccessful/unconfirmed. Their owned data was discarded without signalling stale namespace-relative PIDs. No operator/default database or service was stopped. Logs/manifests and synthetic journal/witness artifacts remain; detailed removal records are `/tmp/ra_w2_owned_cleanup.json` and `/tmp/ra_w2_owned_child_cleanup.json`.

The fake counter is `(complete rendered UTF-8 bytes + 3) // 4` under `synthetic-fixture/1`, with fixed prompt/schema/model/profile bindings and exact/+1 tests. It exercises only the synthetic gate; provider hidden overhead, billing and operational permission remain unproved. Parent journal/witness separation survives child kills but does not establish independent live failure domains or completeness after undetectable rollback of every witness. Unsupported restoration, capacity exhaustion and the 90-day recovery retention horizon stop admission. There is no automatic evidence retirement, refund or live reopening.

## Implemented prerequisite and its limits

[Sandbox helper](../backend/tests/ai/n1_sandbox.py) executes a fresh Python interpreter through bubblewrap with explicit user/PID/network/IPC/UTS isolation and a new mount tree/session. It drops capabilities, disables further user namespaces, clears ambient configuration, mounts the interpreter/stdlib/dependencies and supplied code read-only, and supplies an empty writable `/tmp`. Only the owned database-socket and call-socket directories are visible; parent journal/witness/control directories and home/configuration trees are absent. It rejects code projections containing `.env*`, `.git`, `.codex`, symlinks or excess files/bytes. Descriptor inheritance is closed; stdout/stderr are bounded jointly and failures return fixed codes without raw child error text. Timeouts kill/reap the owned child; no fallback occurs.

The fixed `LD_LIBRARY_PATH=/runtime/native` binds the interpreter's declared library directory read-only. This supports shared Python installations outside `/usr` and was exercised by the successful hosted runs. No ambient `LD_*`, provider settings or administrative credentials enter the child.

[Owned PostgreSQL helper](../backend/tests/ai/n1_postgres.py) creates a separate native PostgreSQL 16 instance for the sandbox test. It never starts an installed/default service. Independent `psql` and `pg_controldata` checks establish database, user, loopback host, port, data directory, system identifier, UTF8, an empty schema and no other clients before schema work. Migration authority uses a generated password over owned loopback TCP outside the child. Unix authentication accepts only the restricted adapter identity/database with SCRAM, then rejects every other identity. Host authentication accepts only the migration role; the isolated child has no host-network path.

The restricted role has no superuser, role/database creation, replication, RLS bypass or inherited role privileges. PUBLIC database privileges and schema creation are revoked; the positive probe receives only SELECT/UPDATE on its fixture table. A separate parent-side test attempts Unix login with the **correct** migration password and verifies HBA rejection. That password is never supplied to the child. The actual W2 privilege checks above separately establish the child’s zero grants on the new tables/functions; the original counter probe alone does not.

[Adversarial tests](../backend/tests/ai/test_n1_isolation.py) check namespace separation, zero capabilities/no-new-privileges, nested-user-namespace denial, parent journal/witness read/write attempts (including `/proc/1/root`), parent-process root visibility, an intentionally inheritable journal descriptor, hidden control-socket access, read-only code, empty temporary storage and absent host loopback connectivity. Numeric PIDs can coincide in different namespaces; the assertion checks namespace/root identity rather than treating a matching number as parent access.

The child successfully updates one synthetic database counter and exchanges a bounded message on its explicitly mounted call socket. It fails twelve privileged SQL operations, three privileged/unknown reconnections and four unauthorized probe messages. The parent independently checks retained journal/witness contents, database effects, endpoint messages and absent control connections. These messages exercise a **probe endpoint**, not the adopted permit issuer or an actual W2 acceptance boundary. They do not prove immutable permit bindings, journal fsync/ack ordering, accounting conservation or guard-loss fencing.

## P2 initialization cleanup correction

The integrated [PostgreSQL cleanup helper/tests](../backend/tests/ai/test_n1_postgres.py) cover startup acknowledgement loss and failures before database creation. Ownership is pinned after initdb by directory device/inode and cluster system identifier; cleanup verifies the maintenance endpoint, exact process/data-directory identity and pidfd before signalling only that owned server. Bounded process exit, absent PID file and durable cluster shutdown must agree. These checks remain active under optimized Python. Unverifiable cleanup preserves the original error and reports `N1_OWNED_DATABASE_CLEANUP_UNRESOLVED`, without exposing credentials or claiming successful shutdown. Nine cleanup cases complement ten isolation cases; no default/shared service is stopped.

## Adopted CI-only AppArmor compatibility policy

The User's explicit adoption applies to **all `/usr/bin/bwrap` executions on the existing temporary hosted `remaining` job's runner**, not just the capability probe. It authorizes the exact Ubuntu `apparmor-profiles` **`4.0.1really4.0.1-0ubuntu0.24.04.7`** member `/usr/share/apparmor/extra-profiles/bwrap-userns-restrict`, profile SHA256 **`11d39094f044f0cda0febb3ad517b830301da6b2ce929664af09ee9e4dd264f9`**, package SHA256 **`bdac5b74d884643653565c52ed7483c9582e646ff72cce8d95d0eb8467a3139c`**. The declared source ABI 4.0/offline feature target is distinct from the exported binary wire ABI. No broader runtime-policy authority or approval identifier is inferred.

### Focused implementation

[The policy helper](../.github/scripts/n1_apparmor_policy.py) uses isolated authenticated Ubuntu APT metadata, lists/cache and configuration for only the pinned artifact. It rechecks version, Package/Architecture, package/member/profile identity, extracts only the approved profile and installs no package scripts or unrelated profiles. Runner APT configuration is unchanged. Loading is restricted to root in a GitHub-hosted Ubuntu 24.04 job; the child never runs under sudo. Existing names, potentially matching attachments (including `/bin/bwrap`), optional local overrides, unknown evidence or inventory drift block loading, with no profile-name whitelist or replacement.

The observed global baseline is preserved exactly: AppArmor enabled **Y**, `apparmor_restrict_unprivileged_userns=1`, `apparmor_restrict_unprivileged_unconfined=0`. No sysctl is changed. The approved enforcing child restriction/capability denial is retained byte-for-byte. The actual capability probe must report **`bwrap//&unpriv_bwrap (enforce)`** and satisfy every namespace/capability/no-new-privileges/nested-userns/read-only/database isolation assertion. Post-test read-only policy/control verification runs whenever loading succeeded, including when the partition failed, and can fail the aggregate.

Preparation/load/verification/probe commands have bounded step and subprocess deadlines. The workflow retains independent PostgreSQL instances, serial tests within each shard, complete/disjoint collections and digest checks, four jobs and fail-closed aggregation. Missing tools or interfaces and further unsupported policy representations fail; they do not broaden this adoption.

## AppArmor loaded-policy identity correction

### Identity chain and fail-closed boundaries

[The guard](../.github/scripts/n1_apparmor_policy.py) uses the kernel's per-profile `raw_data`, `raw_sha256`, `raw_abi` links and `sha256` entry. It verifies that these links identify the same kernel raw-data directory, verifies the complete exported blob's hash and ABI, parses every serialized profile, then binds each current named kernel entry to its exact segment hash, exported attachment and mode. A multi-profile blob can retain segments which are no longer current; a segment alone never establishes that it is loaded. No existing source file or local recompile is substituted for loaded-policy evidence.

[The bounded binary reader](../.github/scripts/n1_policy_binary.py) follows the kernel wire definitions and computes each profile hash over the little-endian version plus the exact serialized profile segment. It interprets the compiled attachment DFA for `/usr/bin/bwrap` and `/bin/bwrap`; any packed accepting permission is conservatively treated as a possible conflict regardless of xattrs or attachment specificity. `<unknown>` remains unresolved until that evidence is verified. All unknown entries, including unrelated applications, are resolved/reported together in each bounded inventory pass. Reserved names, actual attachment matches, optional local overrides, unavailable evidence and mismatches remain blockers. An executable's unconfined label is not used to clear attachment conflicts.

The kernel interface behavior is documented in its [AppArmor filesystem implementation](https://github.com/torvalds/linux/blob/v6.8/security/apparmor/apparmorfs.c), [policy unpacker](https://github.com/torvalds/linux/blob/v6.8/security/apparmor/policy_unpack.c), [profile hashing](https://github.com/torvalds/linux/blob/v6.8/security/apparmor/crypto.c) and [DFA matching](https://github.com/torvalds/linux/blob/v6.8/security/apparmor/match.c). Raw-data reads expose the decompressed original load bytes. The exported raw ABI describes the last header of a complete blob; per-profile hashes retain their own full version. Namespace revisions are read once with nonblocking I/O because that interface is pollable rather than a regular read-to-EOF file.

For the new policy, the guard compiles the hash-verified approved text **once** with the actual hosted compiler and running-kernel features, checks the resulting names, enforcing modes and attachment matches, then submits **those exact compiled bytes** through `--binary --add`. It compares the loaded complete-blob and per-profile hashes with this compilation, including bwrap's own `<unknown>` representation. It records source identity, compiler version/executable hash and compiled identities. Local binary digests are never portable hosted allowlists.

An exclusively created root-owned `/run/n1-bwrap-policy-identity/` directory retains the successful job's compilation, complete unrelated-policy baseline, global controls and post-load revision. The final existing verification step checks this trusted baseline, both intended profiles, unrelated policies and revisions again. Existing state is not overwritten. No policy file is installed in `/etc`, no policy is replaced, and no sandbox payload runs under sudo. The approved restricted child profile, actual enforced child-label assertion, namespace isolation, capability dropping, no-new-privileges, nested-userns denial, read-only projections and restricted database sockets remain unchanged.

Inventories are limited to 4096 profiles and 64 MiB of raw bytes, with 8 MiB per blob, bounded wire depth/node/table sizes, parser command deadlines and the unchanged workflow's outer 30-/20-second load/verification deadlines. The reader supports the packed xmatch representation exercised by the real compiler (wire ABI 5–9); namespace-qualified/renamed blobs, explicit xmatch permission tables, differential xmatch encoding or other unsupported representations fail closed for review. Missing kernel binary-export/hash interfaces also block loading. Compiler/kernel differences are evaluated through actual bytes, not assumed equivalent. No unsupported representation grants an exception or changes a policy.

Kernel `policy` traversal is pinned by a descriptor, with securityfs/AppArmor filesystem-type and namespace identity checks. Canonical objects are opened beneath it with no-follow component traversal; bounded per-profile relative links select candidates whose device/inode/type identities must independently match kernel traversal before reading and after verification. This avoids pathname resolution of magic-link display text and unchecked prefix containment.

The raw ABI reader accepts exact `v5\n` through `v9\n` bytes and compares against the complete blob's last header; malformed, unsupported and mismatched values fail. The source/fixture cross-check covers raw bytes, newline-terminated lowercase raw/profile hashes, ABI, profile name/mode/attachment and pollable decimal revision. See [Linux AppArmor exports](https://github.com/torvalds/linux/blob/v6.8/security/apparmor/apparmorfs.c), [unpacking/last-header assignment](https://github.com/torvalds/linux/blob/v6.8/security/apparmor/policy_unpack.c), [hashing](https://github.com/torvalds/linux/blob/v6.8/security/apparmor/crypto.c) and [the focused tests](../.github/scripts/test_n1_apparmor_policy.py). Tests include real procfs magic-link analogues, source-derived field formats and independent profile-hash calculations; synthetic ABI-mutated blobs are not claimed to have been accepted by a kernel. Diagnostics emit bounded fixed operation/field/category labels and errno, without paths, link targets, evidence bytes or raw OS errors.

## Hosted gate and remaining work

| Verified evidence | Result and scope |
| --- | --- |
| [PR CI 34676651320](https://github.com/runyiy/ai-api-security-platform/actions/runs/34676651320/attempts/1) at reviewed `f5ac48e2a9b4f6ca3c85cd7923f930e1ee5b04b3` | Attempt 1 succeeded: 3817 tests = 117 w2 + 3700 remaining, complete/disjoint collection, actual policy loading, enforcing child confinement and post-test policy/control verification. |
| [Exact-main CI 34677275222](https://github.com/runyiy/ai-api-security-platform/actions/runs/34677275222/attempts/1) at `eb69379910b99cf312cbc9b08f9a3c4b72088c9d` | Attempt 1 succeeded with the same 3817-test and runtime gates. |
| Independent local policy/diagnostic validation | 46 tests passed for the reviewed feature; helpers are outside backend collection. No local AppArmor activation was used. |

The maintenance handoff supplies the independent-review conclusion. Read-only verification during documentation cleanup confirmed PR merge/head, both exact run SHAs, attempt=1, successful job conclusions, complete/disjoint counts, runtime child-success output and post-test verification logs. No CI retry or backend rerun was performed for prose. The ordered collection digest was `976894a7e17540f864924c159ebdf10ff376a99fe58cfd27fce5691b5f3c0031`.

**Scope of the historical hosted runs:** they establish the N1 prerequisite. The later local W2 implementation and its T/X/Y runtime evidence are recorded above and still require independent review; these older hosted runs do not prove that implementation. Input-token/hidden-overhead bounds, account billing/retention, actual budgets and all live operational gates remain unresolved; no provider/Target/private-data permission follows.

## Local validation

The development record contains earlier 3808-test and corrected 3817-node snapshots, targeted N1/cleanup/gate runs and owned-cluster identity/cleanup evidence. They are historical, with the source-snapshot limits retained in [the reviewed record](https://github.com/runyiy/ai-api-security-platform/blob/f5ac48e2a9b4f6ca3c85cd7923f930e1ee5b04b3/docs/research-ai-w2-runtime-validation.md). The integrated PR/main runs above supersede their pending-hosted status. Pure documentation validation requires no application imports or databases; future DB work still requires an independently verified, owned disposable TEST PostgreSQL environment with isolated configuration.

## Historical failed runs and lessons

<a id="hosted-bubblewrap-prerequisite-correction"></a>
<a id="correction-validation-local-only"></a>
<a id="hosted-loopback-restriction-investigation"></a>
<a id="what-the-evidence-establishes"></a>
<a id="authorized-diagnostic-increment"></a>
<a id="actual-local-validation"></a>
<a id="validation-and-remaining-hosted-gate"></a>
<a id="actual-local-validation-and-remaining-hosted-evidence"></a>
<a id="p1-policy-namespace-access-correction--local-pending-independent-review"></a>
<a id="p1-raw-abi-format-correction--local-pending-independent-review"></a>

All runs below failed on attempt 1 and were retained rather than retried to green. Later new commits supplied the corrections integrated by PR #153.

| Run | Failure and lesson |
| --- | --- |
| [34658362313](https://github.com/runyiy/ai-api-security-platform/actions/runs/34658362313/attempts/1) | Missing bubblewrap; ten isolation failures. Runner inventory is not executable/runtime evidence. Add authenticated distribution installation and a required capability probe. |
| [34669278020](https://github.com/runyiy/ai-api-security-platform/actions/runs/34669278020/attempts/1), [34670509334](https://github.com/runyiy/ai-api-security-platform/actions/runs/34670509334/attempts/1) | Loopback `RTM_NEWADDR` failed before child startup. Local WSL success did not establish hosted compatibility. Bounded diagnostics identified relevant controls; missing denial records did not prove the exact denying hook. Later successful adopted-policy runs do not retroactively create that attribution evidence. |
| [34672200107](https://github.com/runyiy/ai-api-security-platform/actions/runs/34672200107/attempts/1) | `<unknown>` attachment rejected before loading. Compiler output can preserve a DFA without its source attachment text; verify complete raw/profile identity and DFA behavior instead of source names or local binary allowlists. |
| [34674561578](https://github.com/runyiy/ai-api-security-platform/actions/runs/34674561578/attempts/1) | 123 profiles failed, zero raw bytes read. Strict pathname resolution cannot reproduce kernel magic-link traversal. Ordinary-directory fixtures concealed the defect; real magic-link regressions and descriptor identity checks were added. |
| [34675661127](https://github.com/runyiy/ai-api-security-platform/actions/runs/34675661127/attempts/1) | 5,691,101 raw bytes read; all 123 profiles failed `validate/raw_abi/malformed_evidence`, before loading/N1 execution. Kernel ABI is `v%d\n`, not a bare integer. Correct fixtures and strict last-header parsing; cross-check all consumed export formats together. The in-memory normalization diagnostic was counterfactual, not committed-source or hosted-load evidence. |

Local setup/cleanup lessons also remain: tool-session lifetime can terminate an owned server between commands; Unix-socket paths must fit the kernel limit; PID numbers alone do not identify processes across namespaces; signal delivery must be observed with bounded pidfd readiness. Historical failed/development attempts are not acceptance runs. The fixed reviewed record retains the detailed evidence without making temporary paths the current documentation interface.
